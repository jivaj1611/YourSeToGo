import {z} from 'zod';export const signupSchema=z.object({name:z.string().trim().min(2).max(80),email:z.string().email().max(254),password:z.string().min(8).max(128)});export const loginSchema=z.object({email:z.string().email(),password:z.string().min(1).max(128)});export const answerSchema=z.object({assessmentId:z.string().min(1),questionId:z.string().min(1),answer:z.string().max(1000).nullable(),responseTimeMs:z.number().int().min(0).max(3600000),tabSwitches:z.number().int().min(0).max(1000).default(0)});export const finishSchema=z.object({assessmentId:z.string().min(1)});
