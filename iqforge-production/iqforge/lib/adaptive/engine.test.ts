import {describe,it,expect} from 'vitest';import {nextDifficulty,chooseQuestion} from './engine';describe('adaptive engine',()=>{it('maps higher theta to harder target',()=>{expect(nextDifficulty(2)).toBeGreaterThan(nextDifficulty(-2))});it('chooses unused item nearest target',()=>{const q=chooseQuestion([{id:'a',difficulty:3},{id:'b',difficulty:5},{id:'c',difficulty:8}],0,new Set(['a']));expect(q?.id).toBe('b')})});
