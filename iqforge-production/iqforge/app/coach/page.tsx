'use client';import {useState} from 'react';import Link from 'next/link';export default function Coach(){const [q,setQ]=useState(''),[answer,setAnswer]=useState(''),[loading,setLoading]=useState(false);async function ask(){setLoading(true);const r=await fetch('/api/coach',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({message:q})});const d=await r.json();setAnswer(d.answer||d.error||'No response.');setLoading(false)}return <main className="min-h-screen"><div className="mx-auto max-w-3xl px-5 py-14"><Link href="/dashboard" className="text-sm text-zinc-500">← Dashboard</Link><h1 className="mt-8 text-5xl font-semibold">AI cognitive coach</h1><p className="mt-4 text-zinc-500">Ask about your actual assessment history. The coach cannot change official scores.</p><textarea value={q} onChange={e=>setQ(e.target.value)} className="focus-ring mt-8 min-h-32 w-full rounded-2xl border border-white/10 bg-white/[.03] p-5" placeholder="Why did I struggle with number sequences?"/><button onClick={ask} disabled={!q||loading} className="mt-3 rounded-xl bg-white px-5 py-3 font-medium text-black disabled:opacity-40">{loading?'Thinking…':'Ask coach'}</button>{answer&&<div className="glass mt-6 rounded-2xl p-6 leading-7 text-zinc-300">{answer}</div>}</div></main>}
