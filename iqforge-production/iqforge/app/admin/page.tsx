
import {redirect} from 'next/navigation';import {getSessionUser} from '@/lib/auth';import {db} from '@/lib/db';export default async function Admin(){const u=await getSessionUser();if(!u||u.role!=='ADMIN')redirect('/dashboard');const [users,questions,assessments,flagged]=await Promise.all([db.user.count(),db.question.count(),db.assessment.count({where:{status:'completed'}}),db.flaggedSession.count({where:{resolved:false}})]);return <main className="min-h-screen"><div className="mx-auto max-w-6xl px-5 py-14"><h1 className="text-5xl font-semibold">Admin</h1><p className="mt-3 text-zinc-500">Operational view of question quality and assessment integrity.</p><div className="mt-10 grid gap-4 md:grid-cols-4">{[['Users',users],['Questions',questions],['Completed',assessments],['Flagged',flagged]].map(([k,v])=><div className="glass rounded-2xl p-6" key={k as string}><p className="text-sm text-zinc-500">{k}</p><p className="mt-3 text-3xl font-semibold">{v as number}</p></div>)}</div><div className="glass mt-6 rounded-3xl p-7"><h2 className="text-xl font-semibold">Question analytics</h2><p className="mt-3 text-sm leading-6 text-zinc-500">Response-level analytics are stored through AssessmentResponse. Calibration views should be built against response count, success rate and response-time distributions before validated norms are introduced.</p></div></div></main>}
