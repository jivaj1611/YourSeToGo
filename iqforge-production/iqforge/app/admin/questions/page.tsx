
'use client';import {useEffect,useState} from 'react';import Link from 'next/link';export default function AdminQuestions(){const [qs,setQs]=useState<any[]>([]);useEffect(()=>{fetch('/api/admin/questions').then(r=>r.json()).then(d=>setQs(d.questions||[]))},[]);async function toggle(q:any){const r=await fetch('/api/admin/questions',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({id:q.id,active:!q.active})});const d=await r.json();if(r.ok)setQs(x=>x.map(y=>y.id===q.id?d.question:y))}return <main className="min-h-screen"><div className="mx-auto max-w-6xl px-5 py-14"><Link href="/admin" className="text-sm text-zinc-500">← Admin</Link><h1 className="mt-8 text-4xl font-semibold">Question bank</h1><div className="mt-8 space-y-3">{qs.map(q=><div key={q.id} className="glass flex items-center justify-between gap-5 rounded-2xl p-5"><div><div className="text-sm text-zinc-500">{q.category} · difficulty {q.difficulty}</div><div className="mt-2 line-clamp-2">{q.prompt}</div></div><button onClick={()=>toggle(q)} className="shrink-0 rounded-lg border border-white/10 px-3 py-2 text-xs">{q.active?'Disable':'Enable'}</button></div>)}</div></div></main>}
