'use client';
import {useEffect,useState} from 'react';import {useRouter} from 'next/navigation';import {Clock3,ShieldCheck,Timer} from 'lucide-react';
export default function Assessment(){const [started,setStarted]=useState(false);const [id,setId]=useState('');const router=useRouter();async function start(){const r=await fetch('/api/assessment/start',{method:'POST'});const d=await r.json();if(r.ok){setId(d.assessmentId);setStarted(true);router.push(`/assessment/${d.assessmentId}`)}}return <main className="min-h-screen"><div className="mx-auto max-w-4xl px-5 py-14"><p className="text-sm text-zinc-500">IQForge assessment</p><h1 className="mt-3 text-5xl font-semibold tracking-tight">A structured test of how you solve problems.</h1><p className="mt-6 max-w-2xl text-lg leading-8 text-zinc-500">Expect roughly 20–25 minutes and adaptive difficulty. You will not see correctness during the assessment.</p><div className="mt-10 grid gap-3 md:grid-cols-3"><Info icon={<Clock3/>} title="~20–25 min" text="Timed sections and response-time capture."/><Info icon={<ShieldCheck/>} title="No assistance" text="No calculator, search or external help."/><Info icon={<Timer/>} title="Adaptive" text="Difficulty changes with performance."/></div><div className="glass mt-6 rounded-3xl p-7"><h2 className="text-xl font-semibold">Before you begin</h2><ul className="mt-5 space-y-3 text-sm leading-6 text-zinc-400"><li>• Answer each item as accurately as you can.</li><li>• Keep the test window active. Visibility changes are recorded as behavioral indicators.</li><li>• We do not record webcam or microphone data.</li><li>• A timeout submits the current item automatically.</li><li>• Your official score is calculated deterministically after submission.</li></ul><button onClick={start} disabled={started} className="focus-ring mt-7 rounded-xl bg-white px-6 py-3 font-medium text-black disabled:opacity-50">{started?'Starting…':'Begin assessment'}</button></div></div></main>}
function Info({icon,title,text}:{icon:React.ReactNode,title:string,text:string}){return <div className="glass rounded-2xl p-5"><div className="text-violet-300">{icon}</div><h3 className="mt-6 font-medium">{title}</h3><p className="mt-2 text-sm leading-6 text-zinc-500">{text}</p></div>}
