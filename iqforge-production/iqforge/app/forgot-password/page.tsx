
'use client';import {useState} from 'react';import Link from 'next/link';export default function Forgot(){const [email,setEmail]=useState(''),[msg,setMsg]=useState(''),[loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);const r=await fetch('/api/auth/forgot',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email})});const d=await r.json();setMsg(d.message||d.error);setLoading(false)}return <main className="grid min-h-screen place-items-center px-5"><div className="w-full max-w-md"><Link href="/" className="text-xl font-semibold">IQ<span className="text-violet-400">Forge</span></Link><div className="glass mt-8 rounded-3xl p-7"><h1 className="text-3xl font-semibold">Reset password</h1><p className="mt-2 text-zinc-500">We'll create a short-lived reset link if the account exists.</p><form onSubmit={submit} className="mt-7 space-y-4"><input className="focus-ring w-full rounded-xl border border-white/10 bg-white/[.03] px-4 py-3" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" required/><button className="w-full rounded-xl bg-white px-4 py-3 font-medium text-black" disabled={loading}>{loading?'Sending…':'Send reset link'}</button></form>{msg&&<p className="mt-5 text-sm text-zinc-400">{msg}</p>}</div></div></main>}
