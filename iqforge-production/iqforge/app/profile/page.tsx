
'use client';import {useEffect,useState} from 'react';import Link from 'next/link';export default function Profile(){const [u,setU]=useState<any>(null);useEffect(()=>{fetch('/api/auth/me').then(r=>r.json()).then(d=>setU(d.user))},[]);return <main className="min-h-screen"><div className="mx-auto max-w-3xl px-5 py-14"><Link href="/dashboard" className="text-sm text-zinc-500">← Dashboard</Link><h1 className="mt-8 text-5xl font-semibold">Profile</h1>{u&&<div className="glass mt-8 rounded-3xl p-7"><div className="grid gap-5"><Field label="Name" value={u.name}/><Field label="Email" value={u.email}/></div><p className="mt-8 text-sm text-zinc-600">Assessment history is private by default. Public leaderboard visibility can be enabled only through explicit account settings.</p></div>}</div></main>}function Field({label,value}:{label:string,value:string}){return <div><p className="text-sm text-zinc-500">{label}</p><p className="mt-2 rounded-xl border border-white/8 bg-white/[.02] p-4">{value}</p></div>}
