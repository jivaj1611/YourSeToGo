'use client';import {Radar, RadarChart, PolarGrid, PolarAngleAxis, ResponsiveContainer,BarChart,Bar,XAxis,YAxis,Tooltip} from 'recharts';export default function Charts({scores}:{scores:Record<string,number>}){const data=Object.entries(scores).map(([subject,A])=>({subject:subject.replaceAll('_',' '),A}));return <div className="mt-6 h-[360px] w-full"><ResponsiveContainer><RadarChart data={data}><PolarGrid stroke="rgba(255,255,255,.1)"/><PolarAngleAxis dataKey="subject" tick={{fill:'#888',fontSize:11}}/><Radar dataKey="A" stroke="#a78bfa" fill="#a78bfa" fillOpacity={.18}/><Tooltip/></RadarChart></ResponsiveContainer></div>}
