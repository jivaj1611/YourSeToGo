
'use client';import {useEffect,useState} from 'react';import Link from 'next/link';export default function Challenge(){const [q,setQ]=useState<any>();const [a,setA]=useState('');const [msg,setMsg]=useState('');useEffect(()=>{fetch('/api/challenge').then(r=>r.json()).then(d=>setQ(d.question))},[]);return <main className="min-h-screen"><div className="mx-auto max-w-3xl px-5 py-14"><Link href="/dashboard" className="text-sm text-zinc-500">← Dashboard</Link><h1 className="mt-8 text-5xl font-semibold">Daily challenge</h1><p className="mt-3 text-zinc-500">One optional reasoning item. Your official assessment score is untouched.</p>{q&&<div className="glass mt-8 rounded-3xl p-7"><h2 className="text-2xl leading-9">{q.prompt}</h2><div className="mt-7 grid gap-3">{q.options.map((o:string)=><button key={o} onClick={()=>setA(o)} className={`rounded-xl border p-4 text-left ${a===o?'border-violet-400 bg-violet-400/10':'border-white/8'}`}>{o}</button>)}</div><button className="mt-6 rounded-xl bg-white px-5 py-3 text-black" onClick={()=>setMsg(a===q.correctAnswer?'Correct. '+q.explanation:'Not quite. '+q.explanation)} disabled={!a}>Check</button>{msg&&<p className="mt-5 text-zinc-400">{msg}</p>}</div>}</div></main>}
