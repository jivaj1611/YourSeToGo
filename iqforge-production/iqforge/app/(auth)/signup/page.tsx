'use client';
import {useState} from 'react';import Link from 'next/link';import {useRouter} from 'next/navigation';
export default function Signup(){const [name,setName]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/auth/signup',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,email,password})});const d=await r.json();setLoading(false);if(!r.ok){setError(d.error||'Unable to create account.');return}router.push('/dashboard')}return <main className="grid min-h-screen place-items-center px-5 py-12"><div className="w-full max-w-md"><Link href="/" className="text-xl font-semibold">IQ<span className="text-violet-400">Forge</span></Link><div className="glass mt-8 rounded-3xl p-7"><h1 className="text-3xl font-semibold">Create your profile</h1><p className="mt-2 text-zinc-500">Your assessment history stays attached to one account.</p><form onSubmit={submit} className="mt-7 space-y-4"><Field label="Name" value={name} set={setName} type="text"/><Field label="Email" value={email} set={setEmail} type="email"/><Field label="Password" value={password} set={setPassword} type="password"/><p className="text-xs text-zinc-600">Use at least 8 characters.</p>{error&&<p className="rounded-xl border border-red-500/20 bg-red-500/5 p-3 text-sm text-red-300">{error}</p>}<button disabled={loading} className="focus-ring w-full rounded-xl bg-white px-4 py-3 font-medium text-black disabled:opacity-50">{loading?'Creating…':'Create account'}</button></form><div className="my-5 flex items-center gap-3 text-xs text-zinc-600"><span className="h-px flex-1 bg-white/8"/>OR<span className="h-px flex-1 bg-white/8"/></div><a href="/api/auth/google" className="focus-ring block rounded-xl border border-white/10 px-4 py-3 text-center text-sm hover:bg-white/5">Continue with Google</a><p className="mt-6 text-center text-sm text-zinc-500">Already have an account? <Link href="/login" className="text-white">Log in</Link></p></div></div></main>}
function Field({label,value,set,type}:{label:string,value:string,set:(v:string)=>void,type:string}){return <label className="block"><span className="mb-2 block text-sm text-zinc-400">{label}</span><input className="focus-ring w-full rounded-xl border border-white/10 bg-white/[.03] px-4 py-3" type={type} value={value} onChange={e=>set(e.target.value)} required minLength={type==='password'?8:undefined}/></label>}
