'use client';
import {useState} from 'react';import Link from 'next/link';import {useRouter} from 'next/navigation';
export default function Login(){const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();setLoading(false);if(!r.ok){setError(d.error||'Unable to sign in.');return}router.push('/dashboard')}return <AuthShell title="Welcome back" subtitle="Continue your cognitive profile."><form onSubmit={submit} className="space-y-4"><Field label="Email" value={email} set={setEmail} type="email"/><Field label="Password" value={password} set={setPassword} type="password"/><div className="flex justify-end"><Link href="/forgot-password" className="text-sm text-zinc-400 hover:text-white">Forgot password?</Link></div>{error&&<p className="rounded-xl border border-red-500/20 bg-red-500/5 p-3 text-sm text-red-300">{error}</p>}<button disabled={loading} className="focus-ring w-full rounded-xl bg-white px-4 py-3 font-medium text-black disabled:opacity-50">{loading?'Signing in…':'Sign in'}</button></form><div className="my-5 flex items-center gap-3 text-xs text-zinc-600"><span className="h-px flex-1 bg-white/8"/>OR<span className="h-px flex-1 bg-white/8"/></div><a href="/api/auth/google" className="focus-ring block rounded-xl border border-white/10 px-4 py-3 text-center text-sm text-zinc-200 hover:bg-white/5">Continue with Google</a><p className="mt-6 text-center text-sm text-zinc-500">New here? <Link href="/signup" className="text-white">Create account</Link></p></AuthShell>}
function Field({label,value,set,type}:{label:string,value:string,set:(v:string)=>void,type:string}){return <label className="block"><span className="mb-2 block text-sm text-zinc-400">{label}</span><input className="focus-ring w-full rounded-xl border border-white/10 bg-white/[.03] px-4 py-3 text-white" type={type} value={value} onChange={e=>set(e.target.value)} required/></label>}
function AuthShell({children,title,subtitle}:{children:React.ReactNode,title:string,subtitle:string}){return <main className="grid min-h-screen place-items-center px-5 py-12"><div className="w-full max-w-md"><Link href="/" className="text-xl font-semibold">IQ<span className="text-violet-400">Forge</span></Link><div className="glass mt-8 rounded-3xl p-7"><h1 className="text-3xl font-semibold">{title}</h1><p className="mt-2 text-zinc-500">{subtitle}</p><div className="mt-7">{children}</div></div></div></main>}
