import Link from 'next/link';import {getSessionUser} from '@/lib/auth';import {redirect} from 'next/navigation';export default async function Practice(){const u=await getSessionUser();if(!u)redirect('/login');return <main className="min-h-screen"><div className="mx-auto max-w-5xl px-5 py-14"><Link href="/dashboard" className="text-sm text-zinc-500">← Dashboard</Link><h1 className="mt-8 text-5xl font-semibold">Practice mode</h1><p className="mt-4 max-w-2xl text-zinc-500">Practice is deliberately separate from official scoring. Pick a domain, train, and review the reasoning immediately.</p><div className="mt-10 grid gap-3 md:grid-cols-3">{['Numerical','Logic','Verbal','Spatial','Memory','Speed'].map(x=><Link href={`/practice/${x.toLowerCase()}`} key={x} className="glass rounded-2xl p-6 hover:bg-white/[.04]"><div className="text-lg font-medium">{x}</div><div className="mt-10 text-sm text-zinc-600">Adaptive · Easy / Medium / Hard</div></Link>)}</div></div></main>}
