import {NextResponse} from 'next/server';export async function GET(req:Request){if(!process.env.GOOGLE_CLIENT_ID||!process.env.NEXT_PUBLIC_APP_URL)return NextResponse.json({error:'Google OAuth is not configured.'},{status:503});const url=new URL('https://accounts.google.com/o/oauth2/v2/auth');url.searchParams.set('client_id',process.env.GOOGLE_CLIENT_ID);url.searchParams.set('redirect_uri',process.env.NEXT_PUBLIC_APP_URL+'/api/auth/google/callback');url.searchParams.set('response_type','code');url.searchParams.set('scope','openid email profile');url.searchParams.set('access_type','offline');return NextResponse.redirect(url)}
