
'use client';import {useSearchParams,useRouter} from 'next/navigation';import {useState} from 'react';export default function Reset(){const sp=useSearchParams(),router=useRouter(),token=sp.get('token')||'', [password,setPassword]=useState(''),[msg,setMsg]=useState('');async function submit(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/auth/reset',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token,password})});const d=await r.json();if(r.ok)router.push('/login');else setMsg(d.error||'Unable to reset password.')}return <main className="grid min-h-screen place-items-center px-5"><div className="glass w-full max-w-md rounded-3xl p-7"><h1 className="text-3xl font-semibold">Choose a new password</h1><form onSubmit={submit} className="mt-7 space-y-4"><input className="focus-ring w-full rounded-xl border border-white/10 bg-white/[.03] px-4 py-3" type="password" minLength={8} value={password} onChange={e=>setPassword(e.target.value)} required/><button className="w-full rounded-xl bg-white px-4 py-3 font-medium text-black">Reset password</button></form>{msg&&<p className="mt-4 text-sm text-red-300">{msg}</p>}</div></main>}
