import {NextRequest,NextResponse} from 'next/server';export function middleware(req:NextRequest){const protectedPaths=['/dashboard','/assessment','/results','/practice','/coach','/profile','/admin'];if(!protectedPaths.some(p=>req.nextUrl.pathname.startsWith(p)))return NextResponse.next();if(!req.cookies.get('iqforge_session'))return NextResponse.redirect(new URL('/login',req.url));return NextResponse.next()}export const config={matcher:['/dashboard/:path*','/assessment/:path*','/results/:path*','/practice/:path*','/coach/:path*','/profile/:path*','/admin/:path*']};
